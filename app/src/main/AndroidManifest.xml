<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/attendease_cmplt_icon_white_bg"
        android:label="@string/app_name"
        android:roundIcon="@drawable/attendease_cmplt_icon_white_bg"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendEase"
        tools:targetApi="31">
        <activity
            android:name="com.bp.attendease.activities.Authentication"
            android:exported="false" />
        <activity
            android:name="com.bp.attendease.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>